# QUESTÃO 01 — [MATRIZ]
# Uma equipe de robótica registrou o desempenho de 3 sensores (linhas) em 3 testes (colunas)
# dentro de uma matriz 3×3 de inteiros.
# Faça um programa que:
# 1. Leia os 9 valores e exiba a matriz em formato de tabela (linhas e colunas). [(check)]
# 2. Calcule e mostre: a soma da diagonal principal[(check)]; a média dos valores da 2a coluna[(check)]; o
# maior valor da 3a linha[(check)].
# 3. Informe a posição (linha, coluna) do menor valor da matriz (indexação a partir de 0).[(check)]

# ENTRADA (EXEMPLO)
# 3 valores por linha (total 9)
# 1 2 3
# 4 5 6
# 7 8 9
# SAÍDA (EXEMPLO)
# Matriz:
# 1 2 3
# 4 5 6
# 7 8 9
# Soma diag. principal: 15
# Média da 2a coluna: 5.00
# Maior da 3a linha: 9
# Menor valor: 1 na posição (0, 0)


# sensor == linha
# testes == coluna
matriz = []
for linha_idx in range(3):
    linha = []
    # a lista linha vai armazenar os valores das linhas . Quando uma linha ficar embaixo de outra linha o valor na posição 0 da linha 1 , vai
    # estar na mesma coluna , que o valor na linha 0 da linha 2 ...
    for coluna_idx in range(3):
        n = int(input(f"sensor ({linha_idx+1}) teste ({coluna_idx+1})"))
        linha.append(n)
    # assim que cada uma das 3 linhas estiver com todos os seus valores (dados no loop das colunas)
    # vamos adicionar cada uma delas à lista matriz
    matriz.append(linha)

print("\t      teste 1       ", "teste 2     ", "   teste 3")
for linha_idx in range(3):
    print(f"sensor {linha_idx} :", end="\t")
    for coluna_idx in range(3):
        print(matriz[linha_idx][coluna_idx], end="   \t\t")
    print()

# soma dos valores da diagonal principal :
soma_diag = 0
# a variável soma_diag vai guardar a soma dos elementos da diagonal principal
soma_coluna2 = 0
# a variável soma_coluna2 vai guardar a soma dos elementos da segunda coluna
maior_valor_3lin = 0
# a variável acima vai guardar o maior valor da 3 linha
for linha_idx in range(3):
    for coluna_idx in range(3):
        if coluna_idx == linha_idx:
            # se o indice da coluna for igual ao indice da linha quer dizer que é um elemento da diagonal principal
            soma_diag += matriz[linha_idx][coluna_idx]
        # vamos usar if ao invés de elif para caso  o item pertença à diagonal principal e também à coluna,ele também seja contabilizado
        if coluna_idx == 1:
            # a segunda coluna num range 3 , é representada pelo indice 1
            # vamos verificar se o valor se encaixa no indice 1 , ou seja , se ele pertence à segunda coluna
            # se sim vamos acumular esse valor na acumuladora soma_coluna2
            soma_coluna2 += matriz[linha_idx][coluna_idx]
    if linha_idx == 2:
        # se for a terceira linha :
        for coluna_idx in range(3):
            if matriz[2][coluna_idx] > maior_valor_3lin:
                maior_valor_3lin = matriz[2][coluna_idx]


# a média da segunda coluna será feita divindo a soma dos elementos por 3 , por que cada coluna tem 3 itens
media_2coluna = soma_coluna2 / 3
maior_valor_matriz = 0
# vamos colocar a variável maior valor como zero , por que é o menor valor possível
posicao = None
# a primeira posicao vai ser none
for linha_idx in range(3):
    for coluna_idx in range(3):
        if matriz[linha_idx][coluna_idx] > maior_valor_matriz:
            maior_valor_matriz = matriz[linha_idx][coluna_idx]
            # agora o valor da variável maior_valor_matriz será matriz[linha_idx][coluna_idx]
            posicao = f"linha {linha_idx} e coluna {coluna_idx}"
            # agora o novo valor da variável posicao será esse novo texto com os valores das linhas e das colunas do novo maior valor
print(f"a soma dos elementos da diagonal principal é {soma_diag}")
print(f"a média dos elementos da segunda coluna é {media_2coluna}")
print(f"o maior valor da 3 linha é {maior_valor_3lin}")
print(
    f"o maior valor da matriz é o elemento {maior_valor_matriz} que se sutua na {posicao}")

